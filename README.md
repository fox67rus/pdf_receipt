# PDF-чек-мейкер

Автоматическая генерация красивых PDF-чеков из CSV данных с поддержкой кириллицы.

## ✨ Особенности

- 📄 Генерация профессиональных PDF-чеков
- 🇷🇺 Полная поддержка кириллицы
- 📊 Автоматический расчет итоговой суммы
- 🎨 Два варианта реализации (ReportLab и WeasyPrint)
- 🖥️ Кроссплатформенность (Windows, macOS, Linux)
- ⚡ Автоматическое открытие PDF после создания

## 📦 Две версии скрипта

### Версия 1: `pdf_maker.py` - Jinja2 + WeasyPrint
- Использует HTML-шаблоны для максимальной гибкости дизайна
- Требует: `jinja2`, `weasyprint`
- ⚠️ **Проблемы на Windows**: WeasyPrint требует установки GTK+ библиотек, что может быть сложно
- ✅ Рекомендуется для Linux/macOS или если нужен более гибкий дизайн через CSS

### Версия 2: `pdf_maker_reportlab.py` - ReportLab (⭐ рекомендуется)
- Прямая генерация PDF без промежуточных шагов
- Требует только: `reportlab`
- ✅ Простая установка одной библиотекой
- ✅ Работает на всех платформах без дополнительных системных зависимостей
- ✅ Автоматическая поддержка кириллицы через системные шрифты

## 🚀 Быстрый старт

### Установка зависимостей

```bash
# Клонируйте репозиторий
git clone https://github.com/fox67rus/pdf_receipt.git
cd pdf_receipt

# Установите зависимости (рекомендуется ReportLab)
pip install -r requirements.txt
```

### Вариант A: ReportLab (рекомендуется)

```bash
pip install reportlab
python pdf_maker_reportlab.py
```

### Вариант B: WeasyPrint

⚠️ **Внимание**: На Windows может потребоваться установка GTK+ библиотек.
Если возникают ошибки с `libgobject-2.0-0`, используйте версию с ReportLab.

```bash
pip install jinja2 weasyprint
python pdf_maker.py
```

PDF автоматически откроется после создания в папке `output/`!

## 📁 Структура проекта

```
.
├── pdf_maker.py              # Версия с Jinja2 + WeasyPrint
├── pdf_maker_reportlab.py    # Версия с ReportLab (рекомендуется)
├── template.html             # HTML-шаблон для версии с WeasyPrint
├── products.csv              # Пример данных для генерации
├── requirements.txt          # Зависимости проекта
├── .gitignore                # Исключения для Git
└── output/                   # Папка с готовыми PDF (создается автоматически)
```

## 📝 Формат CSV

Файл `products.csv` должен содержать следующие колонки:

```csv
product,price,qty
Ноутбук ASUS,85000,2
Беспроводная мышь Logitech,1500,5
USB-C кабель,450,10
```

- `product` - название товара
- `price` - цена за единицу
- `qty` - количество

## 🛠️ Требования

- Python 3.7+
- ReportLab 4.0.7+ (для версии с ReportLab)
- Jinja2 3.1.2+ и WeasyPrint 60.1+ (для версии с WeasyPrint)

## 📄 Пример использования

1. Подготовьте CSV файл с данными о товарах
2. Запустите скрипт:
   ```bash
   python pdf_maker_reportlab.py
   ```
3. PDF будет создан в папке `output/` с временной меткой в имени файла
4. PDF автоматически откроется в системе по умолчанию

## 🔧 Настройка

Вы можете изменить:
- Путь к CSV файлу (по умолчанию `products.csv`)
- Путь к HTML-шаблону (для версии с WeasyPrint)
- Стили и форматирование в коде скрипта

## 🐛 Решение проблем

### Проблема: Черные квадраты вместо русских букв
**Решение**: Используйте версию с ReportLab (`pdf_maker_reportlab.py`) - она автоматически использует системные шрифты с поддержкой кириллицы.

### Проблема: WeasyPrint не запускается на Windows
**Решение**: Используйте версию с ReportLab или установите GTK+ библиотеки для Windows.

### Проблема: Ошибка кодировки в консоли
**Решение**: Скрипты автоматически настраивают UTF-8 для Windows. Если проблема сохраняется, проверьте кодировку вашего терминала.