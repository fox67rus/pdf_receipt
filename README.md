# PDF-чек-мейкер

Автоматическая генерация красивых PDF-чеков из CSV данных.

## 📦 Две версии скрипта

**Версия 1:** `pdf_maker.py` - использует Jinja2 + WeasyPrint (HTML → PDF)
- ⚠️ **Проблемы на Windows**: WeasyPrint требует установки GTK+ библиотек, что может быть сложно
- Рекомендуется для Linux/macOS или если нужен более гибкий дизайн через CSS

**Версия 2:** `pdf_maker_reportlab.py` - использует ReportLab (рекомендуется, особенно для Windows)
- ✅ Простая установка одной библиотекой
- ✅ Работает на всех платформах без дополнительных системных зависимостей

## 🚀 Быстрый старт

### Вариант A: ReportLab (проще)

```bash
pip install reportlab
python pdf_maker_reportlab.py
```

### Вариант B: WeasyPrint (гибче для дизайна)

⚠️ **Внимание**: На Windows может потребоваться установка GTK+ библиотек.
Если возникают ошибки с `libgobject-2.0-0`, используйте версию с ReportLab.

```bash
pip install jinja2 weasyprint
python pdf_maker.py
```

PDF автоматически откроется после создания!

## 📁 Структура проекта

```
.
├── pdf_maker.py      # Основной скрипт
├── template.html     # HTML-шаблон для PDF
├── products.csv      # Данные для генерации
└── output/           # Папка с готовыми PDF
```


